import { EmtpyToNullDirective } from './emtpy-to-null.directive';

describe('EmtpyToNullDirective', () => {
  it('should create an instance', () => {
    const directive = new EmtpyToNullDirective();
    expect(directive).toBeTruthy();
  });
});
